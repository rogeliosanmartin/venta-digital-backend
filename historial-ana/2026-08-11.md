# ANA — 2026-08-11 — Backend Venta Digital

## Objetivo del día

Exponer carátula a Odoo, persistir IDs de ubicación, filtrar cuadrantes por
parque y endurecer reglas de `PATCH /sales/:id/payment`.

## Carátula en anexos Odoo

- Nuevo `DocumentKind.CARATULA`.
- En `signSale`: tras subir a Drive, persiste fila en `sale_documents` con
  `driveFileId` / `driveFileUrl` (base64 se limpia como INE/comprobante).
- `saleToPayload` incluye `documentos.caratulaPdf`.
- `ensureCaratulaFromDrive`: ventas ya firmadas recuperan `{folio}-Caratula.pdf`
  de la carpeta Drive al consultar `GET /integrations/odoo/sales/:id`.

## Ubicación en venta (preasignación)

- Columnas en `Sale`: `park_id`, `section_id`, `quadrant_id`, `space_id`.
- `SalePlanDto` y `applyPayloadToSale` / `saleToPayload` actualizados.
- Fix error de validación: `parkId should not exist` al guardar venta.

## Pago en efectivo

- Columnas `monto_recibido`, `cambio` en `sales`.
- `savePayment` valida y recalcula cambio en servidor (no confía solo en el front).

## Reglas `savePayment` (consolidadas)

| Forma | Requisitos |
|-------|------------|
| Cualquiera | Venta `PENDING_PAYMENT`, precio plan, anticipo > 0, forma válida |
| EFECTIVO | `montoRecibido` ≥ anticipo; limpia cuenta/banco; calcula `cambio` |
| TRANSFERENCIA | `cuenta` + `banco` |
| CHEQUE | `banco`; limpia `cuenta` |

## Proxy Odoo ubicaciones

- `GET /odoo/ubicaciones/cuadrantes` ahora exige `parkId` + `sectionId`
  (proxy a api-odoo-gsm).

## api-odoo-gsm (repo aparte)

- `ubicaciones.repository`: secciones y cuadrantes filtrados por espacios
  vendibles (`available`, `built`, sin `sale_order`), no todos los del catálogo.
- Cuadrantes: firma `searchCuadrantes(parkId, sectionId, …)`.

## DB / migraciones (producción, `DB_SYNC=false`)

```sql
ALTER TABLE sales ADD COLUMN park_id int NULL;
ALTER TABLE sales ADD COLUMN section_id int NULL;
ALTER TABLE sales ADD COLUMN quadrant_id int NULL;
ALTER TABLE sales ADD COLUMN space_id int NULL;
ALTER TABLE sales ADD COLUMN monto_recibido varchar(40) DEFAULT '' NOT NULL;
ALTER TABLE sales ADD COLUMN cambio varchar(40) DEFAULT '' NOT NULL;
```

(Ajustar nombre de tabla si usan prefijo `vd_`.)

## Pendiente / siguiente

- Desplegar backend + api-odoo-gsm.
- Verificar módulo Odoo `sale_order_reception` consume `caratulaPdf` en anexos.
