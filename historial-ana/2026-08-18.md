# ANA — 2026-08-18 — Backend Venta Digital

## Objetivo del día

Predeterminados de vendedor en **tablas propias** (no JSONB) y persistir
sucursal + tipo de servicio en la venta.

## Tablas

- `seller_defaults`: sucursal default del vendedor.
- `seller_default_plans`: planes favoritos (máx. 3 por `planKind`).
- Endpoints: `GET/PATCH /users/me/defaults`.

## Venta

- Columnas `branch_id`, `branch_name`, `service_type_id`, `service_type_name`.
- Mapeo en `sale-form.dto` / `sale.mapper.ts`.
- Finalizar exige sucursal y tipo de servicio.

## Proxy Odoo

- `GET /odoo/sucursales` → `sale.order.branch` (api-odoo-gsm).
- `GET /odoo/tipos-servicio` → `service.type`.

## Odoo

- Expediente: `vd_branch_id`, `vd_service_type_id`.
- Wizard cotización precarga y escribe `branch_id` / `service_type_id` en
  `sale.order`. Dominio sucursal: `company_id` OR `company_id_2`.

## DB (`DB_SYNC=false`)

```sql
ALTER TABLE sales ADD COLUMN service_type_id INT NULL;
ALTER TABLE sales ADD COLUMN service_type_name VARCHAR(180) NOT NULL DEFAULT '';
-- branch_id / branch_name si aún no existen
-- tablas seller_defaults y seller_default_plans
```

## Pendiente / siguiente

- Liberar parque al cancelar expediente sin cotización (19).
