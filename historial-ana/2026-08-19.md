# ANA — 2026-08-19 — Backend Venta Digital

## Objetivo del día

Listado liviano del vendedor y, en Odoo, estatus VD + desapartar al cancelar
expediente sin cotización.

## Listado vendedor

- `findSummariesBySellerId` + `saleToListItem` (sin documentos/payload).
- Fechas ISO seguras. El front ya no filtra por default a 7 días.

## Odoo — cancelar expediente

- Si se cancela **sin cotización** y el espacio está `reserved` (sin
  `sale_order_id`): `park.space.status = available`.
- Chatter + bitácora.
- Si ya hay cotización, `action_cancel` de Odoo libera el parque.

## Odoo — estatus VD

- Campo almacenado `vd_sale_status` a partir de `vd_status`
  (PENDING_PAYMENT, PENDING_SIGNATURE, COMPLETED, REJECTED, DRAFT).
- Árbol: columna «Estatus VD» + filtros/group by.
- Form: badge en header (`is_virtual_file`) y junto a `file_type`
  (`file_type == 'virtual'`).
- Cancelar expediente pone `vd_status = Rechazada`.

## Pendiente / siguiente

- Contrato = `sale.order.name` al generar cotización (24).
